CURRENT BEHAVIOR PLANNER PROMPT

System instruction source:
behavior_planner/system_instruction.txt

Live prompt format:
ID: number
ego = [x, y, v, psi, lane_id, maneuver_possible]
lane = [left_safe, current_safe, right_safe]
route = [next_maneuver, dist_to_signal, signal_phase]
v[id] = [x, y, v, psi, intention, type]

Allowed behaviors:
LANE_KEEP
LANE_CHANGE_LEFT
LANE_CHANGE_RIGHT
STOP
EMERGENCY_BRAKE

Current runtime notes:
- There is no PREV line in the live prompt.
- There is no target_v in the live prompt.
- maneuver_possible is only constrained for left/right turns.
- The current prompt builder emits dist_to_signal = 1000 and signal_phase = G as placeholders.

Current sample prompt:
ID: 12
ego = [-78.5,-1.83,6,0,-3,1]
lane = [1,1,0]
route = [L,120,G]
v[101] = [-41.4,-1.83,2,0,B,0]
v[205] = [-17.7,1.83,7,0,K,0]

Expected response format:
{
  "id": 12,
  "behavior": "LANE_KEEP"
}
